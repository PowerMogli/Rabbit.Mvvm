<UserControl x:Class="SoftCareManager.Views.Application.Desktop.AppMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:appCommands="clr-namespace:SoftCareManager.Common.UI.Commands;assembly=SoftCareManager.Common.UI"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Command="{x:Static appCommands:ApplicationCommands.ChangeShellCommand}"
                CommandParameter="0" Content="Monitor - 1" />
        <Button Grid.Column="1" Command="{x:Static appCommands:ApplicationCommands.ChangeShellCommand}"
                CommandParameter="1" Content="Monitor - 2" />
        <Button Grid.Column="2" Command="{x:Static appCommands:ApplicationCommands.ChangeShellCommand}"
                CommandParameter="2" Content="Monitor - 3" />
    </Grid>
</UserControl>