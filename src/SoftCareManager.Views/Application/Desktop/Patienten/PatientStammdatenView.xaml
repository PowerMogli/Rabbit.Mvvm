<UserControl x:Class="SoftCareManager.Views.Application.Desktop.Patienten.PatientStammdatenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Patient.LastName}"/>
        <TextBlock Grid.Row="1" Text="{Binding Patient.FirstName}"/>
        <TextBlock Grid.Row="2" Text="{Binding Patient.Birthday}"/>
    </Grid>
</UserControl>
